version: "3.9"
services:
  bot:
    build:
      context: ./backend
    container_name: super-trader-bot
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=production
    volumes:
      - ./data:/app/../data
      - ./ui:/app/../ui
    restart: always
